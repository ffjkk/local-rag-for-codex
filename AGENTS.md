# Local RAG project

This project implements the globally registered local_rag MCP service and its document-management UI. Keep application data, dependencies, models, temporary files and tests under the repository root. Use tests/test_library.py for isolated regression tests; do not alter real documents while testing. Global local knowledge retrieval rules apply here as in other projects.
